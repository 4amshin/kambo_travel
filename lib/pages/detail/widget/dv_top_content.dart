import 'package:flutter/material.dart';

class DvTopContent extends StatelessWidget {
  const DvTopContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
